export const metadata = {
  title: "Organization Services | Admin Dashboard",
  description: "Manage your organization's services and offerings",
};

export default function ServicesLayout({ children }) {
  return (
    <div>
      {children}
    </div>
  );
}